#include "main.h"
/**
 * main - entry point
 *
 * Description: print lowercase alphabet a to z
 * Function alpha loop through the alphabet
 * Return: 0;
 */
int main(void)
{
void alpha(void)
{
	char ch;
for (ch = 'a'; ch <= 'z'; ch++)
{
_putchar(ch);
_putchar('\n);
}
return (void);
}
return 0;
}
