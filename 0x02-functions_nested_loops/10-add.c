#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @a: 1st argument
 * @b: 2nd argument
 * Return: the sum of a and b
 */
int add(int a, int b)
{
	return (a + b);
}
