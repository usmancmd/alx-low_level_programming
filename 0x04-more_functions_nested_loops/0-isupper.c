#include <ctype.h>
/**
 * _isupper -  function that checks for uppercase character.
 * @c:  uppercase character.
 * Return: (n);
 */
int _isupper(int c)
{
	if(_isupper(c))
		return (1);
	else
		return (0);
}
