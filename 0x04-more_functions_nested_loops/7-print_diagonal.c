#include "main.h"
/**
 * print_diagonal -  function that draws a diagonal line on the terminal.
 * @n: int n
 * Return (n);
 */
void print_diagonal(int n)
{
	if (n > 0)
	{
	_putchar(\\)_;
	}
	else if (n == 0)
	{
	_putchar('\n');
	}
	else
	{
	_putchar('\n');
	}

}
