#include <stdio.h>
/**
 * main - entry point
 * Return (0);
 */
int main()
{
	int a;

	for (a = 1; a <= 100; a++)
	{
		if (a = a / 3)
		printf("Fizz");
		else if (a = a / 5)
		printf("Buzz")
		else (a = a / 3 && a = a / 5)
		printf("FizzBuzz")
		printf(" ");
	}
}
